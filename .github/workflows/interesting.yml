name: blank

on:
  push:

jobs:
  confidence:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        struggle: ["trust", "mask", "justify", "barely", "nuclear"]
        bleed: ["imaginary", "conflict", "devote", "both"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar CJ
